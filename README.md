# clainstone/tap

Homebrew formulae for [cbirds](https://github.com/clainstone/cbirds), a flock
of birds in your terminal.

```
brew install clainstone/tap/cbirds
```

On macOS and on Linux. `brew upgrade cbirds` updates it, and
`brew uninstall cbirds` removes it.
